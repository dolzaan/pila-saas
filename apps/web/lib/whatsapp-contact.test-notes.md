Manual validation:

1. Configure `NEXT_PUBLIC_PILA_WHATSAPP_NUMBER` with country code and area code.
2. Open the dashboard with a user that has no verified WhatsApp.
3. Confirm the onboarding card opens `wa.me` with the prefilled message.
4. Verify connected users see the compact WhatsApp CTA instead.
